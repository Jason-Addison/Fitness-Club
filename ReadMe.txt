For the client:
Run "npm install" to install packages
Run "npm start" to start browser

For the server:
Run "npm install" to install packages
Run "node Server.js" (make sure node is installed), or just run in IDE

Diagrams in Diagrams.pdf
sql files in server directory

